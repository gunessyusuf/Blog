@{
    ViewData["Title"] = @AppSettings.Title;
}

<div class="text-center">
    @if (!User.Identity.IsAuthenticated)
    {
        <p>Welcome to @AppSettings.Title!</p>
    }
    else
    {
        <p>Welcome to @AppSettings.Title @User.Identity.Name!</p>
    }
</div>
