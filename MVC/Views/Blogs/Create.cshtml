@using System.Security.Claims;

@model BlogModel
@*Generated by ScaffoldApp.*@
@*Model namespace using directive should be added to _ViewImports.cshtml.*@

@{
    ViewData["Title"] = "Create New Blog";
}

<div class="row">
	<div class="col-12 aligncenter">
        <form asp-action="Create" enctype="multipart/form-data">
		@Html.AntiForgeryToken()

			@* Aksiyondan view'a validasyon hatası mesajı taşıma 1. yöntem: *@
			@* @if (ViewBag.CreateMessage is not null)
			{
				<p class="text-danger">@ViewBag.CreateMessage</p>
			} *@
			@* Aksiyondan view'a validasyon hatası mesajı taşıma 2. yöntem: *@
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="row mb-3">
                <label asp-for="Title" class="col-4 col-form-label"></label>
				<div class="col-8">
					<input asp-for="Title" class="form-control" placeholder="Enter title..." />
					<span asp-validation-for="Title" class="text-danger"></span>
				</div>
            </div>
            <div class="row mb-3">
                <label asp-for="Content" class="col-4 col-form-label"></label>
				<div class="col-8">
					<textarea asp-for="Content" class="form-control" rows="3" placeholder="Enter content..."></textarea>
					<span asp-validation-for="Content" class="text-danger"></span>
				</div>
            </div>

			@* @if (User.Claims.SingleOrDefault(c => c.Type == ClaimTypes.Role).Value == "Admin") *@
			@* @if (User.IsInRole("Admin"))
			{
				<div class="row mb-3">
					<label asp-for="Score" class="col-4 col-form-label"></label>
					<div class="col-8">
						<input asp-for="Score" class="form-control" placeholder="Enter score..." />
						<span asp-validation-for="Score" class="text-danger"></span>
					</div>
				</div>
			} *@

            @* <div class="row mb-3">
                <label asp-for="UserId" class="col-4 col-form-label"></label>
				<div class="col-8">
					<select asp-for="UserId" class="form-select" asp-items="ViewBag.UserId">
						<option value="">-- Select --</option>
					</select>
					<span asp-validation-for="UserId" class="text-danger"></span>
				</div>
            </div> *@
			<input asp-for="UserId" type="hidden" />

			<div class="row mb-3">
				<label asp-for="TagIds" class="col-4 col-form-label"></label>
				<div class="col-8">
					<select multiple asp-for="TagIds" class="form-select" asp-items="ViewBag.Tags">
					</select>
					<span asp-validation-for="TagIds" class="text-danger"></span>
				</div>
			</div>

			<div class="row mb-3">
				<label asp-for="Image" class="col-4 col-form-label"></label>
				<div class="col-8">
					<input type="file" name="image" class="form-control" accept="@AppSettings.AcceptedImageExtensions" />
				</div>
			</div>

            <div class="row mb-3">
				<div class="offset-4 col-8">
					<input type="submit" value="Save" class="btn btn-primary" />
					&nbsp;&nbsp;
                    <button type="reset" class="btn btn-outline-primary">Reset</button>
					&nbsp;&nbsp;
					<a asp-action="Index" class="btn btn-outline-primary">Back to Blog List</a>
				</div>
            </div>
        </form>
    </div>
</div>




